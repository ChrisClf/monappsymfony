{% extends 'base.html.twig' %}

{% block title %}ArticleBlog{% endblock %}

{% block body %}
    <h1>ArticleBlog</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article_blog.id }}</td>
            </tr>
            <tr>
                <th>Interview</th>
                <td>{{ article_blog.interview }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_article_blog_index') }}">back to list</a>

    <a href="{{ path('app_article_blog_edit', {'id': article_blog.id}) }}">edit</a>

    {{ include('article_blog/_delete_form.html.twig') }}
{% endblock %}
